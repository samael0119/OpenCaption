<!-- 完整 PRD · v1.0 修订 1；适合大模型读取与版本比较。示意图保留于配套 Word 文档。 -->

# CS 采访实时字幕（OpenCaption）

产品需求文档 PRD · 小米 15 优先

版本  v1.0 · 修订 1   |   日期  2026-09-07   |   状态  开发评审草案

目标用户  中文 CS 观众   |   首发  Android   |   后续验证  iPhone 12

## 产品定义

定位成  **“通用实时字幕翻译工具＋可安装的领域语言包”** 。CS 是第一个语言包，之后可以扩展到其他电竞、体育赛事，甚至技术会议。

场景：用户在电脑或电视上观看 CS 赛前、赛后英文采访，将手机放在外放音源附近，打开 App 即可持续阅读简体中文字幕。源设备无需安装软件，也无需与手机配对。

> 核心价值：听懂选手与主持人表达的关键信息，正确处理口音、CS 术语和选手 ID，并让字幕在整个采访过程中保持可读。

### 已确认与设计假设

- 已确认：英语转中文；主要内容为 CS 赛前、赛后采访；通过手机麦克风听外部设备；小米 15 优先，另有 iPhone 12 可测试。
- 设计假设：首版为个人自用／小规模测试；允许可选云端服务；默认简体中文；正常使用时应用位于前台、屏幕常亮。
- 待验证：两台手机的系统版本、可用内存、联网环境，以及本地模型的速度、准确率与持续功耗。文中的指标均为目标，尚无设备实测结果。

### 关键产品决策

| 决策         | v1.0 取舍                                           |
| ------------ | --------------------------------------------------- |
| 主场景       | 连续英文采访；不以整场激动解说作为发布门槛          |
| 语言与名称   | 固定英语输入；中文输出；选手 ID、战队名默认保留原文 |
| 默认技术路线 | 先用云端链路建立质量基准；本地识别通过验证后可替换  |
| 免费与离线   | 争取低成本；不承诺免费接口无限量或首版完全离线      |
| 首发验收     | 小米 15 为阻塞性测试机；iPhone 12 独立进入下一阶段  |

阅读导航：范围与设备 → 页面与交互 → 功能与状态 → CS 翻译规则 → 技术候选 → 数据与异常 → 指标 → 测试 → 交付与决策 → 依据 → P2 斗鱼同机直播需求。

## 范围、用户任务与设备

### 用户任务

采访开始后快速开启字幕；在下一段回答到来前读懂上一句；遇到选手名或术语时能对照英文；短暂断网后知道哪里漏听；采访结束即可停止收音。

| 优先级           | 范围                                                                                                                    |
| ---------------- | ----------------------------------------------------------------------------------------------------------------------- |
| P0 · 首版必须   | 麦克风授权与音量反馈；开始／暂停／结束；英中字幕；CS 术语与当场名称；可用云端链路；中断与错误提示；会话内回看；诊断指标 |
| P1 · 验证后加入 | 小米本地英语识别；可选离线翻译；语言包管理；会话文本保存与导出；iPhone 12 适配                                          |
| P2 · 后续探索   | 斗鱼同机直播翻译＋悬浮字幕（暂缓验证）；自动名称库更新；自定义词表；更多语言；直接接收电脑音频                          |
| 首版不交付       | 中文配音、跨应用悬浮窗、系统音频抓取、后台／锁屏持续收音、说话人身份识别、账号付费体系、整场解说优化                    |

### 设备与支持策略

| 设备       | 角色             | 约束                                              |
| ---------- | ---------------- | ------------------------------------------------- |
| 小米 15    | Android 首发主机 | 骁龙 8 至尊版；系统、RAM、剩余空间待现场登记 [S1] |
| iPhone 12  | 第二阶段兼容机   | A14 Bionic；系统与电池健康待登记 [S2]             |
| 电脑／电视 | 声音来源         | 外放音频即可；耳机内播放不属于可收音场景          |

设备芯片只作为选型背景，不据此承诺模型速度。首版支持范围先锁定两台实机上登记的系统版本；完成验证后才声明最低系统版本与更广设备兼容性。

### 使用边界

- 建议手机靠近音箱、麦克风无遮挡。初始参考距离 0.3～1 米，按真实收音结果调整；这不是可保证的有效距离。
- 手机不区分“电脑声音”和旁人说话。多个声音重叠时允许漏译或标记未听清，不根据赛况编造答案。
- 首版不对直播进行延迟播放，中文字幕自然落后原声；用户通过历史字幕补看。

## 页面结构与字幕交互

**页面示意（文字版）**

| 页面   | 从上到下的信息布局                                                                             |
| ------ | ---------------------------------------------------------------------------------------------- |
| 首页   | 产品名称、英语转简中 → 放置提示 → 可选战队／选手／地图 → 当前模式及云端处理说明 → 开始字幕 |
| 字幕页 | 收音与联网状态 → 英文小字＋中文大字 → 当前临时句 → 回到最新 → 暂停／结束                   |

配套 Word 文档保留双页面低保真示意图；以上为等价的文字布局说明。

示意图：低保真布局，示例台词为人为构造；仅说明信息层级和行为，不代表已开发页面。

### 信息层级与操作

首页：开始字幕为唯一主按钮；当场信息可跳过；显示当前模式。字幕页：中文为主、英文为辅；固定底部控制区；用文字配合颜色表达状态。设置页：字号、英文显示、主题、服务连接与语言包。

中文默认 24 sp、英文 14 sp；中文可调 20～36 sp。按钮触控区域不小于 48 dp。横屏保留同一信息结构并扩大行宽。深色为默认，支持浅色和系统字号，不用单独颜色表示故障。

最新内容自动滚动；用户上滑后停止跟随，显示“回到最新（N 条）”；点击后恢复。临时句只占一块区域，已确认字幕不因新句到达反复移动或改写。

## P0 功能与会话状态

### F01 · 启动、授权与收音

用户点击开始后请求麦克风权限；首次使用联网模式说明音频／文字将发送到所选服务。拒绝权限时停留首页，提供“去设置”入口，不循环弹窗。完成授权后检查服务可用性，再进入收音。

展示音量活动指示，不能将“有音量”标成“听懂了”。连续 8 秒未检测到人声时显示“未检测到讲话，可将手机靠近音箱”；不中断会话。暂停与结束均须在 1 秒内停止采集和提交新音频。

### F02 · 识别与字幕提交

固定英语。按讲话停顿分段，同时限制长句等待；分段参数在技术验证中调优。识别器支持时显示临时英文；中文默认在稳定短句后提交。若只支持分块返回，不伪装逐字流式结果。

每句有稳定 ID。临时结果在原句位置替换；确认结果追加一次；重试与乱序返回不得重复、倒序或覆盖已确认字幕。超过 10 秒仍无译文时，该句显示“翻译超时”，保留英文并继续后续句。

### F03 · 暂停、回看与结束

暂停时冻结收音、保留字幕；已提交请求可在 3 秒内完成，超时取消。恢复时从当前声音继续，并标记暂停间隔。结束时停止新输入，最多等待 3 秒收尾，取消剩余请求，进入本次字幕回看。

| 状态           | 进入条件                 | 用户可见行为                   |
| -------------- | ------------------------ | ------------------------------ |
| 就绪／准备中   | 首页；授权或服务检查     | 开始按钮／准备提示；可取消     |
| 收音中         | 采集成功                 | 音量活动、持续字幕、暂停／结束 |
| 等待讲话       | 8 秒无人声               | 提示靠近音源；继续监听         |
| 已暂停         | 用户暂停或应用离开前台   | 显示暂停；返回后手动继续       |
| 恢复连接       | 网络或服务短暂失败       | 标记中断；保留已有字幕         |
| 已结束／不可用 | 用户结束；授权或服务失败 | 回看；显示可执行的修复入口     |

切后台、锁屏、来电或其他录音抢占时，首版暂停收音；回来后明确提示继续。录音期间仅维持屏幕常亮，不要求用户修改系统全局省电设置。

## CS 术语与翻译质量规则

### F04 · 当场信息与词表

预置精简 CS 术语表；用户可在开始前填入战队、选手 ID 和地图。首版不依赖自动抓取赛事名单。会话仅注入相关词条，避免把所有选手名塞入每次请求。名称提示只提高识别倾向，不保证结果正确。

| 表达                  | 默认含义／输出       | 边界                           |
| --------------------- | -------------------- | ------------------------------ |
| trade / trade kill    | 补枪／补枪击杀       | 涉及人员交易时按原意           |
| eco round / force buy | 经济局／强起         | 不要按一般财经词义翻译         |
| save                  | 保枪                 | save money 译为省钱            |
| utility               | 道具                 | 结合具体烟、闪、火的上下文     |
| execute               | 战术进攻／爆弹进攻   | 按整句决定，不强制逐词替换     |
| rotation / retake     | 转点或回防／回防夺点 | 区分时间点与攻守语境           |
| clutch / opening duel | 残局／开局对枪       | 不擅自添加“一打几”           |
| 选手 ID、战队、地图   | 优先保留标准原文     | 未知名称保留识别文本并允许回看 |

### F05 · 翻译约束

- 上下文提供最近 3～5 句已确认英文，初始上限 800 个英文词；只输出当前句译文。暂停、断流超过 30 秒或更换采访后清空上下文。
- 保留否定、可能性、因果、比分和数字。不得补写比赛结局、选手身份、发言对象或未提及的战术。
- 识别文本、历史文本均作为待翻译内容，不执行其中的指令；译文不得输出分析过程、问候或额外解释。
- 不要向用户显示模型自报的“准确率”。无可靠结果时用“未听清／暂未译出”，保留可用原文。

### 词条数据与验收样例

词条字段：term_id、英文表达、别名、中文建议、适用语境、保留原文标志、版本。内置词表随 App 版本更新；当场信息仅本次会话有效。

构造样例：We needed to trade better, but we decided to save in the next round.
期望：我们需要把补枪做得更好，但下一回合我们决定保枪。
负例：I wanted to save money. 必须译为“我想省钱”，不能误译为保枪。

## 技术路线与模型选型门槛

产品接口保持可替换。先做通用收音、字幕状态和词表，再通过同一测试集比较云端、本地和混合链路；不把模型名称写死在用户主界面。

| 模式         | 处理链路                             | 发布策略                                     |
| ------------ | ------------------------------------ | -------------------------------------------- |
| 联网增强     | 手机收音 → 云端英语识别 → 云端翻译 | P0；初始质量基准，需真实网络验证             |
| 本地识别增强 | 手机英语识别 → 云端文本翻译         | P1；小米 15 通过延迟、准确率和功耗门槛后开放 |
| 离线基础     | 手机英语识别 → 手机文本翻译         | P1 实验；语言包完整且通过质量门槛才能启用    |

### 候选能力，不等同于已选供应商

ASR 候选：云端可先评估 Whisper large-v3／turbo 服务或支持词表适配的识别接口；本地比较 whisper.cpp 的 base.en、small.en 及量化版本。英语专用小模型有官方依据，但口音与远场效果仍需设备录音实测 [S3、S4]。

翻译候选：云端选择支持上下文和术语约束的快速大模型；离线先测 ML Kit。ML Kit 为免费端侧 SDK，模型按需下载，主要面向日常翻译，不能预设其 CS 术语质量达标 [S5、S6]。4B 本地大模型不列为 P0 依赖。

小米路径：先采用标准音频采集与可移植推理库；不能因具备 NPU 就假设模型自动获得加速。iPhone 路径：优先复用业务规则、词表和协议；音频生命周期与推理后端单独适配。

### 建议工程边界

- 客户端建议 Flutter 承载页面与会话逻辑，通过原生桥接采集、音频会话和本地推理；具体插件、授权和系统兼容由 M0 验证。无法满足延迟时替换桥接实现，不改产品协议。
- 云端网关只承担服务适配、鉴权、限流与短时请求，不需要工作流调度集群。开发者服务密钥保存在服务端；若走自用 BYOK，使用系统安全存储。
- 统一音频输入为可配置的单声道 PCM；初始采用 16 kHz，仅在所选识别器支持时使用。按服务要求重采样，禁止只改采样率标签。

### 决策门槛

只有满足“成功指标与非功能验收”一节的首发质量与稳定性目标，并在“测试集与验证计划”一节的保留测试集上复现，才能设为默认模式。离线失败时仍可发布可用的联网版，并明确功能范围。免费额度不构成架构依赖。

## 数据、异常恢复与成本边界

### 最小数据契约

Session：会话 ID、开始／结束时间、模式、英语→简中、词表版本、当场信息、状态。Segment：会话 ID、句子 ID、音频起止时间、修订号、原文、译文、临时／确认／失败状态、结果显示时间。

Adapter：接受音频或文本请求；返回部分／最终结果与标准化错误。所有返回携带会话 ID、句子 ID 和修订号；客户端忽略旧会话与过期修订。提示词和后端版本记录在诊断中，便于复现。

### F06 · 故障恢复规则

| 故障                     | 系统行为                                                                                                 |
| ------------------------ | -------------------------------------------------------------------------------------------------------- |
| 网络断开                 | 最多保留最近 10 秒待处理音频于内存；自动退避重连。更旧内容丢弃并插入“此处收音未完成”，防止延迟无限积累 |
| 服务 429／额度耗尽       | 短时限流按服务重试提示退避，受队列上限约束；明确额度耗尽则停止请求并提示，不自动切换付费服务             |
| 识别有结果、翻译失败     | 显示英文与失败标记，后续句继续；恢复后不自动重翻全部历史                                                 |
| 麦克风不可用／被打断     | 立即暂停；说明原因；恢复后由用户继续，不在后台静默开启                                                   |
| 模型缺失／损坏／内存不足 | 该本地模式不可选；允许重新下载或主动切换已配置模式                                                       |
| 推理持续落后             | 显示“处理跟不上”；限制队列、标明遗漏区间；提供切换模式，不静默丢句                                     |

### 数据保留与联网说明

P0 默认不落盘原始音频；内存环形缓冲在消费、结束或退出后释放。字幕仅留本次会话，离开回看页前提示未保存内容将清除。P1 可增加本地保存、单条／整场删除与用户主动导出。

诊断默认只包含时延、状态、错误码和资源统计，不含音频、字幕、密钥。测试录音需显式开启；云端处理受所选供应商条款约束，不承诺供应商零留存。离线模式不得调用识别／翻译云端接口。

### 成本控制

按有效讲话时长、音频计费单位、文本 token 和重试统计成本；有最新单价时显示估算，否则显示用量与“费用未知”。单次预算为可配置项，实际额度由服务端校验；到限暂停云端处理。免费层不足时清晰提示，用户可暂停或主动更换已配置服务 [S7、S8]。

## 成功指标与非功能验收

以下是 v1.0 的拟定验收目标，尚未实测。M0 可以提出调整，但必须记录基线、原因与产品影响，不能将调低指标当作验证通过。首发门槛针对小米 15 的默认模式。

| 指标               | 目标                             | 测量方法                                                                   |
| ------------------ | -------------------------------- | -------------------------------------------------------------------------- |
| 信息理解率         | 至少 90%                         | 人工标注核心命题：谁、做了什么、原因、态度；译文正确保留的命题数／总命题数 |
| CS 术语正确率      | 至少 90%                         | 对人工标注的语境术语计算正确译法出现数／总出现数                           |
| 已提供名称正确率   | 至少 90%                         | 当场信息中实际说出的名称，按允许别名匹配；漏掉算错                         |
| 关键反义／数字错误 | 发布样本中为 0                   | 已显示字幕不得把否定变肯定，或改变比分、人数与关键比较关系                 |
| 中文字幕延迟       | P50 ≤ 3秒；P95 ≤ 6秒           | 可对齐短句末尾音频在手机被采集，到该句稳定中文首次显示                     |
| 长句首条可读字幕   | 90% 的连续长句 ≤ 8秒            | 从语音开始计时；单独度量，不能用句末延迟掩盖长等待                         |
| 启动准备           | 热启动 P95 ≤ 3秒；冷启动 ≤ 8秒 | 已授权、资源齐全；点击开始到可采集音频，不含首次下载                       |
| 连续运行           | 60分钟无崩溃／ANR／不可恢复停止  | 3 次完整运行；每 10 分钟统计延迟，不能持续累积                             |
| 字幕一致性         | 确认句重复、乱序为 0             | 含重连、重试、暂停恢复与模式切换的脚本化测试                               |

### 资源与网络测试条件

基准：手机正常室温、不充电、固定 50% 亮度、同一 Wi-Fi；记录系统版本、RAM、剩余空间、电池健康和音量。网络仿真增加 150 ms RTT、1% 丢包与 10 秒断连。所有延迟同时报告成功覆盖率，超时／丢句不得从结果中隐藏。

小米首版建议资源预算：联网模式 60 分钟耗电不高于 15 个百分点；本地模式候选不高于 20 个百分点、进程峰值内存不高于 1.5 GB。均属工程预算，须经实机确认；发生系统严重热状态必须提示并允许停止。

iPhone 12 在第二阶段独立设立资源基线与门槛，不要求与小米运行相同模型；同样遵守准确性和不编造原则。支持大字号、横屏、安全区域与读屏按钮标签。

## 测试集与验证计划

### 数据集构建

准备 30 段采访片段，每段 1～2 分钟，总计约 45 分钟。覆盖至少 10 位说话人；包括清晰英语、不同非母语口音、现场噪声、密集 CS 术语及选手／战队／地图名称。另加 5 分钟纯音乐、观众声与静音负例。

按说话人和采访场次分为开发集 20 段、保留测试集 10 段；避免同一回答切片跨集合。每段记录原始视频链接、时间码与来源；评测使用权需满足来源要求，原始赛事片段不随 App 分发。

每段同时保留直接音轨作为诊断基准，以及手机对电脑外放的重录版本作为产品验收输入。手机实录是最终依据，不能用干净音轨成绩代替。

### 测试矩阵

| 维度     | 基准组                | 挑战组                           |
| -------- | --------------------- | -------------------------------- |
| 声音来源 | 电脑外放；约 0.5米    | 约 1米／2米；较低音量            |
| 节目内容 | 安静采访、单人发言    | 场馆背景声、插话、长句、口音     |
| 设备     | 小米 15，登记系统版本 | iPhone 12 第二阶段同源比较       |
| 运行模式 | 云端识别＋云端翻译    | 本地识别混合；离线基础           |
| 网络     | 稳定 Wi-Fi            | 蜂窝、断连、限流、额度耗尽       |
| 生命周期 | 持续前台 60分钟       | 来电、锁屏、切后台、音频路由变化 |

### 标注与分析规则

- 英文参考稿由人工对照音频校验；预先标注核心命题、关键否定／数字、术语和可接受名称别名。听不清的原始内容单独标记，不强行造参考答案。
- WER 用于定位识别错误：忽略标点与大小写，保留数字、名称和否定词；另报名称及术语错误率。中文以含义为主，不要求逐字贴合单一参考译文。
- 原型阶段可由用户主评；发布前关键错误由第二位熟悉 CS 且能听懂英文的人复核。对分歧记录裁决，不把模型自评当作最终真值。
- 标准环境需满足“成功指标与非功能验收”一节的全部门槛；挑战组单独报告，不与基准混合稀释成绩。静音／纯音乐负例应为零虚构采访译文。

产出：逐段结果、时延分布、资源曲线、失败样例、词表开关 A/B 对照。先定位收音／识别／翻译哪个环节出错，再决定是否加大模型。

## 交付计划、风险与待决策项

工期假设：1 名有移动开发经验的开发者全职、有 AI 编程辅助、可使用两台实机；以下为工程估算，不含商店审核和供应商账户开通等待。

| 阶段               | 估算                | 输出与进入下一阶段条件                                          |
| ------------------ | ------------------- | --------------------------------------------------------------- |
| M0 · 质量验证     | 3～5 个工作日       | 数据集初版、云端基准、小米本地 ASR 对比；确认实际可达服务与成本 |
| M1 · Android 闭环 | 5～8 个工作日       | 可安装 APK；前台收音、字幕、词表、会话状态及中断恢复            |
| M2 · 自用测试版   | 5～8 个工作日       | 完成保留集、3 次长稳测试；修复严重误译与丢句；交付已知问题表    |
| M3 · 本地能力     | 追加 5～10 个工作日 | 按门槛选择混合／离线模式；未通过则保持实验状态                  |
| M4 · iOS 验证     | 追加 5～10 个工作日 | iPhone 12 同场景验收、签名安装／测试分发；资源与模式独立决策    |

### 主要风险与收敛策略

- 口音／噪声导致识别失真：保留英文、靠近音源、对照较强 ASR 基准；术语表不能替代收音质量。
- 本地模型快但译意差：不以模型小或免费作为唯一选择标准；默认模式必须同时通过理解率和时延门槛。
- 免费服务及联网环境变化：M0 在实际使用网络验证连续会话；服务可替换，预算透明，不承诺永久免费。
- Google 语言包下载在目标网络不可达：M0 专项验证；不把 ML Kit 设为首版强依赖，可更换离线引擎。
- 范围控制：斗鱼同机直播、后台处理与悬浮字幕已列入 P2 储备，见末尾专项需求；暂不验证 APK、不阻塞首版。中文配音、整场解说等另行评审。

### 待决策记录（不阻塞本 PRD）

D1 设备：测试时登记小米 RAM、两台系统版本、电池健康。D2 联网：验证常用网络，决定供应商和是否采用 BYOK。D3 费用：M0 提供每小时估算后，由用户确定云端预算。D4 模式：M0/M3 根据真实结果选择默认链路。D5 iOS：进入 M4 前确认可用 Mac、签名和分发方式。

交付物：本 PRD 对应的安装包、最小运行配置说明、模型／服务版本清单、人工评测结果、已知问题与回退说明。未达到离线门槛的能力应标注实验，不纳入宣传承诺。

## 资料依据与术语说明

资料核查日期：2026-09-07。引用用于证明平台／模型能力，不代表本项目性能已经达标。API 模型、免费额度、许可证与支持系统可能变化，接入时须再次确认。

[S1 · 小米官方：Xiaomi 15 常见问题（芯片与设备信息）](https://www.mi.com/hk/support/faq/details/KA-536391/)

[S2 · Apple：iPhone 12 技术规格（A14 Bionic）](https://support.apple.com/en-us/111876)

[S3 · OpenAI Whisper：英语专用模型与模型能力](https://github.com/openai/whisper)

[S4 · whisper.cpp：端侧实现、量化与集成示例](https://github.com/ggml-org/whisper.cpp)

[S5 · ML Kit：端侧翻译能力与质量边界](https://developers.google.com/ml-kit/language/translation)

[S6 · ML Kit Android：翻译器与模型下载管理](https://developers.google.com/ml-kit/language/translation/android)

[S7 · Gemini API：价格、免费层与数据使用说明](https://ai.google.dev/gemini-api/docs/pricing)

[S8 · Groq：请求、token 与音频额度限制](https://console.groq.com/docs/rate-limits)

[S9 · Google Cloud Speech-to-Text：词表适配](https://docs.cloud.google.com/speech-to-text/docs/adaptation-model)

[S10 · Android：麦克风录音能力与权限](https://developer.android.com/media/platform/mediarecorder)

[S11 · Apple：麦克风实时语音识别示例](https://developer.apple.com/documentation/speech/recognizing-speech-in-live-audio)

### 术语说明

| 术语         | 本文件含义                                           |
| ------------ | ---------------------------------------------------- |
| ASR          | 将英语语音识别为英文文字                             |
| VAD          | 检测人声活动，辅助分段；不等于理解内容               |
| 稳定字幕     | 满足提交条件后固定显示的句子；区别于可修订的临时结果 |
| WER          | 英文单词错误率，用于识别诊断，不替代译意评价         |
| P50 / P95    | 50% / 95% 的观测值不超过该值                         |
| BYOK         | 用户提供自己的服务 API 密钥                          |
| P0 / P1 / P2 | 首版必须／后续优先／后续探索                         |
| M0～M4       | 交付里程碑；并非模型版本                             |

> 评审结论栏：产品范围、质量指标与预算由需求方评审；技术候选以真实录音和两台手机验证结果决定。当前版本已给出默认推进方案，待决策项不影响开始 M0。

## P2 储备需求：斗鱼同机直播翻译

> 修订记录：根据用户反馈新增本需求。优先级 P2；状态为已纳入需求、暂缓验证与开发。不加入 M0 必做项，不影响现有 P0／P1 排期和首版发布。

### 已知结果与能力边界

用户反馈：小米 15 使用系统录屏时，斗鱼直播能够录到声音。当前仅确认系统录屏路径有声音；尚未验证普通第三方 App 的内部音频捕获、悬浮显示或后台稳定性，也未分别确认耳机与外放。

按用户决定，本阶段不制作或安装测试 APK。系统录屏结果不能作为第三方采集可用的验收依据；实际开发前再进行专项验证。当前不声明已支持斗鱼同机翻译。

### 目标体验与功能范围

P2-DY-01 · 入口与授权：新增“翻译本机直播”入口，原模式标为“听外部设备”。功能未交付前不显示可启动入口。进入同机模式时再申请内部音频采集和悬浮窗授权。

P2-DY-02 · 声音与翻译：优先使用斗鱼内部音频，复用英语识别、CS 术语表、上下文翻译与字幕状态。无法捕获时明确提示不可用，不静默切换麦克风。

P2-DY-03 · 悬浮字幕：半透明字幕区域，支持拖动、字号／透明度调整、收起、暂停与结束。适配斗鱼横屏全屏，避免遮挡比分和主要控制按钮。

P2-DY-04 · 后台运行：用户主动启动后，切到斗鱼仍继续处理，保留运行通知。此模式对“切后台暂停”规则作专项扩展；锁屏、来电及采集被撤销时仍暂停并提示。

P2-DY-05 · 平台边界：首个候选组合仅为小米 15＋斗鱼；记录系统、斗鱼版本与音频路由。其他直播 App 和 iPhone 同机悬浮字幕不随本需求默认纳入。

### 后续启动条件与验收

待核心采访字幕可用、且用户重新安排此需求后，再启动第三方 APK 验证。验证需覆盖：内部音频实际可回听、横屏全屏悬浮可见、直播操作不受妨碍、外放与耳机分别测试、连续运行 30 分钟不中断。

基础验证通过后再接入模型，复测准确率、端到端延迟、发热与耗电。未通过则保持 P2 储备状态，不绕过来源 App 的捕获限制。上述测试是未来启用条件，不是本阶段待执行任务。
